var React = require('react');

var ProductsList = React.createClass({
  render: function(){
    return(

      <div>
        {productsList}
      </div>
    );
  }
});

module.exports = ProductsList;
